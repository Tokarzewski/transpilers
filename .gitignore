__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
.venv_cuda/
out/
train_*.log
dist/
build/
*.egg-info/
src/transpilers/llm/cache/*
!src/transpilers/llm/cache/.gitkeep
.direnv/

# Fortran .mod files generated by gfortran
*.mod


.claude/

# generated sweep artifacts
scripts/ep_*.json

# --- run-portability: exclude huge non-essentials ---
# 12GB vendored research dataset; re-clone separately if needed
third_party/CodePivot/
# Only the shipped adapter is tracked; experimental adapters live on Hugging Face.
data/sft/cpp_mojo/adapter_*/
!data/sft/cpp_mojo/adapter_15b_v2/
# training optimizer state (~141MB each), not needed for inference
data/sft/cpp_mojo/adapter_*/checkpoint-*/
*.pt
# intermediate reasoning batches + scratch outputs
data/sft/cpp_mojo/_reason_out/
artifacts/

# LLaMA Factory / LLaMA Board GUI runtime cache + run outputs (created in cwd when running the webui)
llamaboard_cache/
saves/

# Mojo corpus: raw third-party shallow clones (license + bloat).
# Only the manifest + README are committed.
data/mojo_corpus/
# corpus build scratch (regenerable)
data/build_corpus_list.py
data/clone_corpus.sh
data/analyze_corpus.py
# dedup/analysis intermediate caches (regenerable from data/dedup_corpus.py et al.)
data/_dedup_cache.json
data/_clean_aux.json
data/_repo_signals.json

# --- data flywheel (issue #51) ---
# Append-only repair outcomes log: regenerable by re-running the crawl/repair
data/repair_outcomes.jsonl
# Latest metrics snapshot: re-derived from repair_outcomes.jsonl
data/flywheel_metrics.json
# Crawl output: re-derivable by re-running scripts/crawl_github.py
data/sft/github_crawl/
# SFT pairs harvested from verified repairs: re-derivable by promote_repair.py
data/sft/flywheel_pairs.jsonl
# Promotion idempotency log: regenerable by re-running
data/sft/flywheel_promotions.jsonl
# Auto-rendered metrics markdown: re-derived from data/flywheel_metrics.json
docs/flywheel_metrics.md

# secrets / embedded vendored clone (added 2026-06-19)
.env
stateless transpilation/.env
transpyle/

# large regenerable cbm-derived dependency graph (rebuild: migration_plan.py --cbm)
data/sft/cpp_mojo/dep_graph_cbm.json

# graphify cache (generated)
graphify-out/
